/*const http = require("http");
const server = http.createServer((req, res) => {
  if (req.url === "/") {
    res.write("Your home page is already available");
    res.end();
  } else if (req.url === "/another") {
    res.write("I'm another route");
    res.end();
  } else {
    res.write("I'm listening");
    res.end();
  }
});
Port = 3000;
server.listen(Port, () => {
  console.log(`Server started on port ${Port}`);
});
*/
